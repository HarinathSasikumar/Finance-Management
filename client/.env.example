# Copy this to .env.local for local development (Vite proxy handles /api, so leave empty)
# In production (Render/Vercel), set this to your deployed backend URL:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
